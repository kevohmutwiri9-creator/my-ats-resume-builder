# Render.com Static Site Service
services:
  - type: web
    name: ats-resume-builder
    env: static
    rootDir: .
    buildCommand: ""
    staticPublishPath: .
    
# Environment variables
envVars:
  - key: NODE_ENV
    value: production
